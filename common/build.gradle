archivesBaseName = "ModLauncherUtils-Core"

dependencies {
    api project(":api")
}

publishing {
    publications {
        maven(MavenPublication) {
            from components.java
        }
    }
}
